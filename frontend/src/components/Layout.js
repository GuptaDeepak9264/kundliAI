import { Link, NavLink, useNavigate } from 'react-router-dom';
export default function Layout({children}) { const navigate=useNavigate(); const user=JSON.parse(localStorage.getItem('kundli_user')||'null'); const logout=()=>{localStorage.clear();navigate('/login')}; return <><header><Link className="brand" to="/">✦ Kundli <em>AI</em></Link><nav><NavLink to="/dashboard">Dashboard</NavLink><NavLink to="/generate">New Kundli</NavLink><NavLink to="/history">History</NavLink><NavLink to="/profile">Profile</NavLink>{user?.is_admin&&<NavLink to="/admin">Admin</NavLink>}<button className="link-button" onClick={logout}>Logout</button></nav></header><main>{children}</main></> }
